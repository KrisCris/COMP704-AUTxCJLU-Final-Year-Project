Detect Food
---
tags:
  - FOOD
parameters:
  - in: formData
    name: food_b64
    type: string
    required: true
    description: base64 encoded image
  - in: formData
    name: rotation
    type: string
    required: true
    description: camera rotation on the flutter in fixing rotation issues
responses:
  1:
    description: Successful
    schema:
      properties:
        code:
          type: number
          description: return code
          example: 1
        msg:
          type: string
          description: return info
          example: 'success'
        data:
          type: array
          items:
            type: object
            example: {"basic":{"img": "anB64Image", "name": "rawName"}, "info":"json obj of food info"}
